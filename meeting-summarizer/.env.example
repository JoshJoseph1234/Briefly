# === AI Provider (use Groq by default) ===
GROQ_API_KEY=
# Optional: override model
GROQ_MODEL=llama3-70b-8192

# === Optional OpenAI fallback ===
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# === Email via Resend (recommended for simplicity) ===
RESEND_API_KEY=
RESEND_FROM="Meeting Summarizer <onboarding@resend.dev>"

# === OR Email via SMTP (fallback if no Resend) ===
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Meeting Summarizer <no-reply@example.com>"
